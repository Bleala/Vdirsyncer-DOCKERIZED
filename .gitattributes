# Set the default behavior, in case people don't have core.autocrlf set
* text=auto

# Explicitly handle files that MUST have LF on Linux/macOS
*.sh          text eol=lf linguist-detectable=false
*.py          text eol=lf
*.js          text eol=lf
*.json        text eol=lf
*.yaml        text eol=lf
*.yml         text eol=lf
Dockerfile    text eol=lf
.dockerignore text eol=lf

# Explicitly handle files that MUST have CRLF on Windows
*.bat         text eol=crlf
*.cmd         text eol=crlf

# Mark binary files to prevent Git from changing them
*.png         binary
*.jpg         binary
*.jpeg        binary
*.gif         binary
*.ico         binary
*.pdf         binary
